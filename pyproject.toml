[build-system]
requires = [
    "setuptools",
    "versioningit",
]
build-backend = "setuptools.build_meta"

[tool.versioningit]

[tool.ruff]
line-length = 119

[tool.ruff.lint]
preview = true
select = ["F", "E", "W", "NPY", "UP", "I", "RUF027"]
